
  rustfs:
    image: rustfs/rustfs:latest
    container_name: rustfs-server
    # build:
    #   context: .
    #   dockerfile: Dockerfile.source
    #   args:
    #     TARGETPLATFORM: linux/amd64
    ports:
      - "9000:9000" # S3 API port
      - "9001:9001" # Console port
    environment:
      - RUSTFS_VOLUMES=/data/rustfs{0..3} # Define 4 storage volumes
      - RUSTFS_ADDRESS=0.0.0.0:9000
      - RUSTFS_CONSOLE_ADDRESS=0.0.0.0:9001
      - RUSTFS_CONSOLE_ENABLE=true
      - RUSTFS_CORS_ALLOWED_ORIGINS=*
      - RUSTFS_CONSOLE_CORS_ALLOWED_ORIGINS=*
      - RUSTFS_ACCESS_KEY=rustfsadmin
      - RUSTFS_SECRET_KEY=rustfsadmin
      - RUSTFS_OBS_LOGGER_LEVEL=info
      - RUSTFS_TLS_PATH=/opt/tls
    #  - RUSTFS_OBS_ENDPOINT=http://otel-collector:4318
    security_opt:
      - "no-new-privileges:true"
    volumes:
      - ${APP_DATA_PATH_HOST}/rustfs/data/pro:/data
      - ${APP_LOG_PATH_HOST}/rustfs/logs:/app/logs
      - ${APP_DATA_PATH_HOST}/rustfs/data/certs/:/opt/tls # TLS configuration, you should create tls directory and put your tls files in it and then specify the path here
    networks:
      - backend
    restart: unless-stopped
    healthcheck:
      test:
        [
          "CMD",
          "sh", "-c",
          "curl -f http://127.0.0.1:9000/health && curl -f http://127.0.0.1:9001/rustfs/console/health"
        ]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    # depends_on:
    #   otel-collector:
    #     condition: service_started
    #     required: false
